{
  "name": "sprintos-local-baseline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "backup": "node scripts/create-local-backup.mjs",
    "safety:check": "node scripts/pre-commit-secret-check.mjs",
    "baseline:report": "npm run safety:check && npm run backup && git status --short",
    "repo:baseline": "npm run baseline:report"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^6.0.0"
  }
}
